
# upstream 参照用の一時クローン・使い捨てビルドスクリプト (コミット対象外)
.tmp_*

# 実機書き込み用のビルド成果物。build-output/ にまとめる。
# ビルドし直せば作れるものなので、コミットしない。
build-output/
*.uf2
*.bin

# スライドを画像に起こしたもの (scripts/render-slides.py で作り直せる) と、
# その際にデッキの隣に一時的に置かれるコピー。
docs/presentation/renders/
docs/presentation/.render-*.tmp.html

# Spresense のフラッシュイメージ (docker cp で取り出し直せる)
*.spk

# Oversized source photos: the deck ships the downscaled copies in assets/
docs/presentation/assets/spresense+esp-s3.jpg
